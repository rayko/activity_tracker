<%= box_container do %>
  <div class="bg-gray-300 rounded-t-md py-2 text-lg text-gray-500 shadow-sm/20">
    <%= "#{@activity.id} - #{@activity.name}" %>
  </div>
  <% content_for :title, "Activities - #{@activity.name}" %>
  <%= render  @activity %>
<% end %>
